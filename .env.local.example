
# The site's public origin. Used as metadataBase so link-preview images
# resolve to absolute URLs — without it, a deployed build advertises
# og:image URLs pointing at its own localhost and forwards unfurl with no
# image. Leave unset locally (it falls back to http://localhost:3000).
NEXT_PUBLIC_SITE_URL=https://whatsnearyou.example
